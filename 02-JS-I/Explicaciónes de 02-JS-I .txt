VARIABLES:

-Son una forma de guardar distintos tipos de datos para ser usados posteriormente.

STRINGS:

-Son un tipo de datos que contienen cadenas de texto.

FUNCIONES:

-Es un proceso donde se manipulan informacion con el objetivo de devolver un tipo de datos.

FUNCIONES(ARGUMENTOS):

-Son datos que se introducen en las funciones para que �stas las usen.

FUNCIONES(RETURN):

-Es un objeto usado en las funciones para devolver un tipo de dato despu�s de parar inmediatamente la funci�n

DECLARACIONES (IF):

-Es una operaci�n de control de flujo, �sta operaci�n verifica si se cumple una condici�n determinada. Si se cumple entonces se ejecutan las instrucciones establecidas en dicho caso, pero si no se cumple la condici�n ignorar� las instrucciones establecidas dentro de la operaci�n de control.

VALORES BOOLEANOS:

-Es un tipo de dato que verifica si una declaraci�n es verdadera o falsa, solo tiene dos posibles datos "true" o "false". Estos dos posibles datos seran devueltos por una funci�n despu�s de verificar la informaci�n con operadores de comparaci�n.